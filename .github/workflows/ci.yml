name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Build, test, typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.24.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Test
        run: pnpm --filter @clarxai/engine test

      - name: Typecheck
        run: pnpm --filter @clarxai/engine typecheck && pnpm --filter @clarxai/cli typecheck

      - name: Score (dogfood)
        run: node packages/cli/dist/cli.js score . --min-score 80

      # Scores pinned real repositories and diffs against committed snapshots.
      # Clones ~8 public repos at depth 1; the whole run is a few seconds.
      # A failure here means engine behaviour changed on real code — if the
      # change is intended, re-run locally with --update and commit the diff.
      - name: Corpus regression
        run: pnpm --filter @clarxai/corpus corpus
